# AI 工程知识库

> 创建于 2026-08-14。本文件是知识库宪章，也是人类与 Agent 共同遵守的运行契约。

## 定位与范围

本知识库面向 AI 工程实践，整理智能体框架、长期记忆、多智能体系统、模型与运行时配置、自动化运维及相关研究经验。

知识库分为两层：

- 来源层：`raw/sources/**`。它镜像旧仓库根目录结构，集中保存迁入的原始文档，作为事实、配置、过程和研究材料的不可变、可追溯来源。
- 知识层：`wiki/**`。它按主题重构来源内容，消除文件布局差异，提供稳定入口、交叉链接和面向问题的知识页面。

来源文件迁入 `raw/sources/**` 后不得因专题重构而删除或静默改写。新增原文按其来源路径进入该目录；需要修订原文时，应保留变更记录，并同步刷新引用它的专题页。

## 受众与成功问题

主要读者是建设、集成和运维 AI Agent 系统的开发者、研究者与自动化 Agent。

本知识库应能稳定回答：

1. 一个 AI 工程概念、平台或框架的核心机制、边界和实现方式是什么？
2. OpenClaw 等系统如何配置、运行、排障并维护记忆与多智能体能力？
3. 某项结论、配置或操作建议来自哪些原始文档，当前状态是否仍然有效？

## 知识契约

- 可追溯：每个 `wiki/**` 页面必须通过 `sources` 使用相对路径引用来源层文件；精确事实和强断言必须能够回到原文核对。
- 集中保留：所有来源统一存放在 `raw/sources/**`，目录结构镜像迁移前路径；知识重构不得删除、覆盖或静默改写用户原文。
- 可重编译：`wiki/**` 是可更新的知识层，每页必须有稳定 ID、类型、状态、日期、责任人和来源。
- 明确不确定性：推断、冲突、过期和未知必须明确标注；证据不足时不得用模型先验补齐。
- 原子提交：每次知识变更应聚焦单一主题，来源引用、知识正文和导航同步更新，避免将无关改动混入同一提交。
- 严格审计：进入活动导航的页面必须通过结构、元数据、来源路径和链接完整性审计；严格审计失败时不得视为完成。
- 查询隔离：临时查询结果保存在 `outputs/queries/**`，达到晋升标准后才可进入 `wiki/**`。

## 页面模型

支持的页面类型包括 `concept`、`entity`、`source-summary`、`how-to`、`reference`、`explanation`、`decision` 和 `archive`。

文件名使用 kebab-case；`id` 是稳定身份，不随标题变化。页面应围绕一个明确主题组织，而不是机械复制来源目录。

## 检索与回答

1. 从 `wiki/index.md` 选择主题入口。
2. 使用全文检索定位相关知识页并读取上下文。
3. 对精确事实、配置值和高风险操作回到页面 `sources` 指向的来源核对。
4. 检查页面状态、新鲜度、冲突和未知项后再形成回答。
5. 证据不足时明确回答未知，不使用模型先验补齐事实。

## 新鲜度与责任

- 默认审查周期：180 天。
- 配置、依赖、接口和外部平台等高频变化知识：30 天。
- 页面责任人由 frontmatter 的 `owner` 指定；未指定时不得进入活动导航。

## 审计契约

知识库使用 `llm-wiki-expert` 的 manifest 与 audit 工具执行严格审计。审计应至少覆盖必需元数据、来源可达性、内部链接、页面状态与新鲜度；执行命令和具体技能路径由当前环境提供，不在知识页中硬编码。

## 未决问题

- 尚无统一外部发布渠道；当前以仓库内 Markdown 为权威版本。
